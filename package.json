{
  "name": "redux-saga-debounce-effect",
  "description": "A debounce effect for redux-saga",
  "main": "dist/index.js",
  "version": "0.2.0",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:madewithlove/redux-saga-debounce-effect.git"
  },
  "keywords": [
    "redux-saga",
    "debounce",
    "effects"
  ],
  "author": {
    "name": "Madewithlove",
    "email": "heroes@madewithlove.be"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^2.4.0",
    "eslint-config-madewithlove": "^3.1.1",
    "expect": "^1.18.0",
    "madewithlove-webpack-config": "^4.0.0",
    "mocha": "^2.4.5",
    "redux": "^3.5.1",
    "redux-saga": "^0.10.0",
    "webpack": "^1.12.14"
  },
  "peerDependencies": {
    "redux-saga": "^0.10.0"
  },
  "scripts": {
    "build": "BABEL_ENV=production webpack --progress",
    "prepublish": "npm run build",
    "postversion": "git push; git push --tags; npm publish",
    "lint": "eslint src",
    "test": "mocha tests --recursive --compilers js:babel-core/register",
    "test:watch": "npm run test -- --watch"
  }
}
